Probar multilabel con crosentropy (varias labels -> camara+accion) vs con label normal (1 accion cada vez) categorical vs binary

Espera entre acciones para obtener una imagen identificativa
    - Antes de realizar la accion

No realizar un balanceo de datos

Hacer imitation learning

Probar una clase por movimiento de camara vs una clase con varias carmaras

Hacer que los vectores sean un valor por accion vs multilabel
